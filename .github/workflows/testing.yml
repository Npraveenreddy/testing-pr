name: Deploy 
on:
  push:
    branches:
      - test 

env:
  CURRENT_BRANCH: ${{ github.ref }}

jobs:  
  build_push:  
    runs-on: ubuntu-latest  
    container:
      image: openjdk:8-jdk-slim 
    steps:
      - uses: actions/checkout@v1
      - run: echo "hello"
      - run: git clone -b $CURRENT_BRANCH https://github.com/Npraveenreddy/front-end2.git
      - run: ls -lrt